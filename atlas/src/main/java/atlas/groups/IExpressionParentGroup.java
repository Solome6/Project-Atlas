package atlas.groups;

/**
 * Children type of a IFileChildrenGroup to represent the various parents an expression can have.
 * 
 * Even though this doesn't expose any new features it allows for cleaner organization between classes. 
 */
public interface IExpressionParentGroup extends IFileChildrenGroup {

}
